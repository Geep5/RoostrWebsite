// Same flags as /app: the stepper talks to the paired daemon and the
// browser's pairing storage, neither of which exists at prerender time.
export const ssr = false;
export const prerender = false;
